
/**
 *
 * @param state
 * @param action
 * @returns {{isLoggedIn: boolean}}
 */
const testReducer = (state = {
    isLoggedIn: false
}, action) => {

    return state;
};

export default testReducer;